//This one is a abstract class.
abstract class Car {
  void engine(
      String
          onOff); //And this is a abstract method (unimplemented method). Only abstract class can contain abstract method
  void sitSpace(
      String
          totalPassenger); //Abstract classes can contrain both(abstract methods and normal methods).
}

class Speed {}
